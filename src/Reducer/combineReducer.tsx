import { combineReducers } from 'redux';
import reducer from '../Reducer/reducer';

const rootReducer = combineReducers({
    // in the new state of javascript key and value will be same
    reducer
});
export default rootReducer;